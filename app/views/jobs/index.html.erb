<div class="row">
  <div class="col-sm-8 col-sm-offset-2 jobs-col">
    <h1>Jobs</h1>
    <ul>
      <% @jobs.each do |job| %>
        <li><%= link_to job.title, job_path(job) %></li>
      <% end %>
    </ul>
  </div>
</div>
